import { VStack } from 'native-base';

import AddForm from '@components/AddForm';

const NewAdd = () => {
    
    return (
        <VStack flex = {1}>
            <AddForm />
        </VStack>
    )

}

export default NewAdd;